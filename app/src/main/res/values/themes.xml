<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.LayananDesa" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="fontFamily">@font/poppins</item>
        <item name="android:textStyle">normal</item>
        <item name="backgroundColor">@color/white</item>
        <item name="textFillColor">@color/black</item>
    </style>

    <style name="BottomSheetDialogTheme" parent="Theme.Design.Light.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/BottomSheetStyle</item>
    </style>

    <style name="BottomSheetStyle" parent="Widget.Design.BottomSheet.Modal">
        <item name="android:background">@android:color/transparent</item>
    </style>

    <style name="BottomNavView" parent="Widget.MaterialComponents.BottomNavigationView">
        <item name="itemBackground">@android:color/transparent</item>
        <item name="itemRippleColor">@android:color/transparent</item>
        <item name="itemTextColor">@color/bottom_nav_colors</item>
        <item name="itemIconTint">@color/bottom_nav_colors</item>
    </style>

    <style name="Theme.LayananDesa" parent="Base.Theme.LayananDesa" />

    <style name="Theme.Simple" parent="Theme.Material3.Light">
        <!-- Custom Tema Simple untuk orang tua -->
        <item name="android:textSize">18sp</item>
        <item name="colorPrimary">@color/colorPrimary</item>
    </style>

    <style name="Theme.Modern" parent="Theme.Material3.Light">
        <item name="android:textSize">14sp</item>
        <item name="colorPrimary">@color/colorPrimary</item>
    </style>
</resources>